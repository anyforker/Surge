# AdBlock script mirrors

本目录由 `scripts/sync-adblock-modules.sh` 自动生成，用于保存 AdBlock 模块依赖的上游脚本。

文件名由上游文件名规范化为小写 kebab-case，并追加源 URL 的短哈希以避免同名冲突。完整的本地文件与上游地址映射见 `SOURCES.tsv`，请勿手动修改生成文件。

脚本版权与许可归各上游作者所有；本目录仅作同步镜像，具体使用条款以对应源仓库为准。
